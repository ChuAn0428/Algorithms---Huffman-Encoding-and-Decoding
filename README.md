# Algorithms---Huffman-Encoding-and-Decoding
Input a text file, count the frequency of each character, build the binary tree, generate the code table, encode the text file, output in binary level, rebuild the tree, decode the binary file, output the text.
